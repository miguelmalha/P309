@model P309.Models.Conta

@{
    ViewBag.Title = "Eliminar";
}

<h2>Eliminar utilizador</h2>

<h3>Tem a certeza de que deseja eliminar este utilizador?</h3>

@*
    BeginForm() - Criação de um formulário para aceitar dados do utilizador. Utilização do método POST e processamento realizado pelo método de ação do controlador
    ActionLink() - retorna para a ação inserida
    TextBox() - Campo para inserção de um atributo do tipo texto
    DisplayFor() - exibição do valor do campo
    PagedListPager() - faz o output da paginação
*@

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Nome de Utilizador  @* Martelado pois DisplayFor não permite escolher um nome para o item a mostrar *@
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NomeDeUtilizador)
        </dd>

        <dt>
            Palavra-Passe  @* Martelado pois DisplayFor não permite escolher um nome para o item a mostrar *@
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PalavraPasse)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Role)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Role)
            <label>
                (0:Administrador, 1:Visualizador)
            </label>
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">@* Botões Eliminar e voltar*@
            <input type="submit" value="Eliminar" class="btn btn-default" /> |
            @Html.ActionLink("Voltar", "Acessos")
        </div>
    }
</div>
